import type { Metadata, Viewport } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Scout Platform v5.2 | Executive Analytics Dashboard',
  description: 'Real-time analytics and insights dashboard powered by Medallion+ architecture',
  keywords: ['analytics', 'dashboard', 'scout', 'executive', 'insights'],
  authors: [{ name: 'Scout Platform Team' }],
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  );
}